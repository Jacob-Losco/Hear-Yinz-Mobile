//this is a sample view to test login navigation, it should be removed upon completion of the first map view control

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Replace with map view")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
